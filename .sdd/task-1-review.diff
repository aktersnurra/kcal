# Review package: 4fad4ad738e913fa2d77f51331c07046c1217359..fc7b8133a3108a499892cc0defbf4e49d9655e29

## Commits
fc7b8133a310 feat(auth): preserve validated token scopes

## Files changed
lib/auth.ml                |  9 ++++++++-
lib/http_adapter.ml        |  6 +++---
lib/oidc.ml                | 19 ++++++++++++++++++-
test/test_auth.ml          | 41 +++++++++++++++++++++++++++++++++++------
test/test_http.ml          |  2 +-
test/test_withings_http.ml |  2 +-
6 files changed, 66 insertions(+), 13 deletions(-)

## Diff
diff --git a/lib/auth.ml b/lib/auth.ml
index b8a56b21b9..47d9bfcb5d 100644
--- a/lib/auth.ml
+++ b/lib/auth.ml
@@ -1,3 +1,7 @@
+type identity = { user : User.t; scopes : string list }
+
+let has_scope identity scope = List.mem scope identity.scopes
+
 type t = {
   resolve_user : issuer:string -> subject:string -> (User.t, Error.t) result;
   verifier : Oidc.t;
@@ -8,4 +12,7 @@
 let authenticate_bearer auth token =
   match auth.verifier token with
   | Error () -> Error Error.Unauthorized
-  | Ok claims -> auth.resolve_user ~issuer:claims.issuer ~subject:claims.subject
+  | Ok claims ->
+      Result.map
+        (fun user -> { user; scopes = claims.scopes })
+        (auth.resolve_user ~issuer:claims.issuer ~subject:claims.subject)
diff --git a/lib/http_adapter.ml b/lib/http_adapter.ml
index 679a7f761f..f2a1f7fddb 100644
--- a/lib/http_adapter.ml
+++ b/lib/http_adapter.ml
@@ -36,7 +36,7 @@
   | None -> plain 401 "Unauthorized"
   | Some token ->
       (match Auth.authenticate_bearer auth token with
-      | Ok user -> f user
+      | Ok identity -> f identity.user
       | Error Error.Unauthorized -> plain 401 "Unauthorized"
       | Error _ -> plain 500 "Internal Server Error")
 
@@ -107,8 +107,8 @@
           (match Auth.authenticate_bearer auth token with
           | Error Error.Unauthorized -> plain 401 "Unauthorized"
           | Error _ -> plain 500 "Internal Server Error"
-          | Ok user ->
-              (try json 200 (Yojson.Safe.to_string (mcp_handle withings ~service ~user (Yojson.Safe.from_string body)))
+          | Ok identity ->
+              (try json 200 (Yojson.Safe.to_string (mcp_handle withings ~service ~user:identity.user (Yojson.Safe.from_string body)))
                with Yojson.Json_error _ ->
                  json 400 (Yojson.Safe.to_string (Mcp.rpc_error (-32700) "Parse error"))))
       | _, None -> plain 401 "Unauthorized"
diff --git a/lib/oidc.ml b/lib/oidc.ml
index 78a65ad176..af883dfa4a 100644
--- a/lib/oidc.ml
+++ b/lib/oidc.ml
@@ -3,6 +3,7 @@
   subject : string;
   audience : string list;
   expires_at : Ptime.t;
+  scopes : string list;
 }
 
 type clock = unit -> Ptime.t
@@ -50,6 +51,15 @@
       | _ -> None)
   | _ -> None
 
+let scopes_of_payload = function
+  | `Assoc fields ->
+      (match List.assoc_opt "scope" fields with
+      | Some (`String scopes) ->
+          String.split_on_char ' ' scopes
+          |> List.filter (fun scope -> scope <> "")
+      | _ -> [])
+  | _ -> []
+
 let expiration_of_payload = function
   | `Assoc fields ->
       (match List.assoc_opt "exp" fields with
@@ -72,7 +82,14 @@
          && subject <> ""
          && List.mem audience token_audience
          && Ptime.compare expires_at now > 0 ->
-      Ok { issuer = token_issuer; subject; audience = token_audience; expires_at }
+      Ok
+        {
+          issuer = token_issuer;
+          subject;
+          audience = token_audience;
+          expires_at;
+          scopes = scopes_of_payload jwt.Jose.Jwt.payload;
+        }
   | _ -> Error ()
 
 let key_for_token keys token =
diff --git a/test/test_auth.ml b/test/test_auth.ml
index 9066919b7a..90c809568b 100644
--- a/test/test_auth.ml
+++ b/test/test_auth.ml
@@ -1,15 +1,19 @@
 let time seconds = Option.get (Ptime.of_float_s seconds)
 
 let claims ?(issuer = "https://issuer.example") ?(subject = "alice")
-    ?(audience = [ "kcal-client" ]) ?(expires_at = time 2_000_000_000.) () =
-  Oidc.{ issuer; subject; audience; expires_at }
+    ?(audience = [ "kcal-client" ]) ?(expires_at = time 2_000_000_000.) ?(scopes = []) () =
+  Oidc.{ issuer; subject; audience; expires_at; scopes }
 
 let verifier results = Oidc.of_verified_claims (fun _ -> results)
 
-let signed_token ?(issuer = "https://issuer.example") ?(audience = "kcal-client") ?(expires_at = 2_000_000_000) () =
+let signed_token ?(issuer = "https://issuer.example") ?(audience = "kcal-client")
+    ?(expires_at = 2_000_000_000) ?(scopes = "") () =
   let key = Result.get_ok (Jose.Jwk.of_priv_json_string {|{"kty":"oct","k":"c2VjcmV0","kid":"test","alg":"HS256"}|}) in
   let header = Jose.Header.make_header key in
-  let payload = Printf.sprintf {|{"iss":"%s","sub":"alice","aud":"%s","exp":%d}|} issuer audience expires_at in
+  let payload =
+    Printf.sprintf {|{"iss":"%s","sub":"alice","aud":"%s","exp":%d,"scope":"%s"}|}
+      issuer audience expires_at scopes
+  in
   Jose.Jws.sign ~header ~payload key |> Result.get_ok |> Jose.Jws.to_string
 
 let oidc ?(max_age_s = Some 60) ~clock () =
@@ -28,6 +32,15 @@
   ignore (verifier (signed_token ()));
   Alcotest.(check int) "expired cache refetched" 2 !fetches
 
+let test_oidc_preserves_validated_scopes () =
+  let verifier, _ = oidc ~clock:(fun () -> time 1_000.) () in
+  let token = signed_token ~scopes:"ledger:read withings:manage" () in
+  match verifier token with
+  | Ok claims ->
+      Alcotest.(check (list string)) "scopes"
+        [ "ledger:read"; "withings:manage" ] claims.scopes
+  | Error () -> Alcotest.fail "valid token rejected"
+
 let test_oidc_rejects_bad_tokens_and_claims () =
   let verifier, _ = oidc ~clock:(fun () -> time 1_000.) () in
   List.iter (fun token -> Alcotest.(check bool) "rejected" true (Result.is_error (verifier token)))
@@ -35,6 +48,20 @@
   let token = signed_token () in
   Alcotest.(check bool) "tampered" true (Result.is_error (verifier (token ^ "x")))
 
+let test_auth_returns_user_and_scopes () =
+  let store, _ = Test_support.store_with_user () in
+  let auth =
+    Auth.make ~resolve_user:(Store_sqlite.resolve_user store)
+      ~verifier:(verifier (Ok (claims ~scopes:[ "ledger:read" ] ())))
+  in
+  match Auth.authenticate_bearer auth "valid-token" with
+  | Ok identity ->
+      Alcotest.(check bool) "read scope" true
+        (Auth.has_scope identity "ledger:read");
+      Alcotest.(check bool) "write scope" false
+        (Auth.has_scope identity "ledger:write")
+  | Error _ -> Alcotest.fail "valid token rejected"
+
 let test_rejects_expired_token () =
   let store, _ = Test_support.store_with_user () in
   let auth =
@@ -51,8 +78,8 @@
   let auth = Auth.make ~resolve_user:(Store_sqlite.resolve_user store) ~verifier:(verifier (Ok (claims ()))) in
   let first = Result.get_ok (Auth.authenticate_bearer auth "valid-token") in
   let second = Result.get_ok (Auth.authenticate_bearer auth "valid-token") in
-  Alcotest.(check string) "stable user" (User_id.to_string first.id)
-    (User_id.to_string second.id)
+  Alcotest.(check string) "stable user" (User_id.to_string first.user.id)
+    (User_id.to_string second.user.id)
 
 let test_rejects_unverified_token () =
   let store, _ = Test_support.store_with_user () in
@@ -85,7 +112,9 @@
     [
       ( "authentication",
         [ Alcotest.test_case "OIDC signed JWT and cache" `Quick test_oidc_verifies_signed_jwt_and_cache;
+          Alcotest.test_case "OIDC preserves validated scopes" `Quick test_oidc_preserves_validated_scopes;
           Alcotest.test_case "OIDC rejects malformed and bad claims" `Quick test_oidc_rejects_bad_tokens_and_claims;
+          Alcotest.test_case "user and scopes" `Quick test_auth_returns_user_and_scopes;
           Alcotest.test_case "expired token" `Quick test_rejects_expired_token;
           Alcotest.test_case "stable verified identity" `Quick test_resolves_verified_identity_stably;
           Alcotest.test_case "unverified token" `Quick test_rejects_unverified_token;
diff --git a/test/test_http.ml b/test/test_http.ml
index 6c8ba29835..dbba73a1cd 100644
--- a/test/test_http.ml
+++ b/test/test_http.ml
@@ -1,5 +1,5 @@
 let auth store =
-  let claims = Oidc.{ issuer = "https://issuer.example"; subject = "alice"; audience = [ "kcal-client"; ]; expires_at = Option.get (Ptime.of_float_s 2_000_000_000.) } in
+  let claims = Oidc.{ issuer = "https://issuer.example"; subject = "alice"; audience = [ "kcal-client"; ]; expires_at = Option.get (Ptime.of_float_s 2_000_000_000.); scopes = [] } in
   Auth.make ~resolve_user:(Store_sqlite.resolve_user store)
     ~verifier:(Oidc.of_verified_claims (fun _ -> Ok claims))
 
diff --git a/test/test_withings_http.ml b/test/test_withings_http.ml
index ec91789a14..2e4acb8811 100644
--- a/test/test_withings_http.ml
+++ b/test/test_withings_http.ml
@@ -1,5 +1,5 @@
 let auth store =
-  let claims = Oidc.{ issuer = "https://issuer.example"; subject = "alice"; audience = [ "kcal-client" ]; expires_at = Option.get (Ptime.of_float_s 2_000_000_000.) } in
+  let claims = Oidc.{ issuer = "https://issuer.example"; subject = "alice"; audience = [ "kcal-client" ]; expires_at = Option.get (Ptime.of_float_s 2_000_000_000.); scopes = [] } in
   Auth.make ~resolve_user:(Store_sqlite.resolve_user store) ~verifier:(Oidc.of_verified_claims (fun _ -> Ok claims))
 
 let integration ?(sync = fun _ _ -> Ok ()) store =
